/**
 * Domain types, the single source of truth, imported by the math core, the schema
 * layer, and the UI. Plain TypeScript with no runtime dependencies.
 *
 * @packageDocumentation
 */

export * from './state.ts'
export * from './advice.ts'
export * from './stats.ts'
