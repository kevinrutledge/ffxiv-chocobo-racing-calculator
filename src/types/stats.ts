/** Display names of the five attributes, in UI order (index 0 to 4). */
export const STAT_NAMES = ['Maximum Speed', 'Acceleration', 'Endurance', 'Stamina', 'Cunning'] as const

/** Default dump attribute: Acceleration (index 1). */
export const DEFAULT_DUMP_INDEX = 1
