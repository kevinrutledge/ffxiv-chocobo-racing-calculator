/** Shared Tailwind class strings for the input controls. */

/** A small stepper button (used by the rank and attribute inputs). */
export const STEP_BUTTON = 'rounded bg-navy/60 px-2 py-1 text-sm tabular-nums transition-colors hover:bg-navy disabled:opacity-30'

/** A compact numeric text field, centred and tabular. */
export const NUMBER_FIELD = 'rounded bg-hud px-2 py-1 text-center tabular-nums text-cream'

/** A bordered panel surface (used for attribute rows and the results panel). */
export const PANEL = 'rounded-md border border-navy/40 bg-panel/60'

/** A secondary bordered button (reset, dialog Cancel); callers add the text colour. */
export const SECONDARY_BUTTON = 'rounded border border-navy/60 px-3 py-1 text-sm transition-colors hover:border-navy'
